(* Written by Eric Hall, under the guidance of Michael Norrish *)

open HolKernel Parse boolLib bossLib;

val _ = new_theory "convolutional_codes";

(* Properties we need to prove *)
(*                             *)
(* Takes an input binary sequence and outputs a binary sequence     *)


(* Given an arbitrary HMM and an arbitrary sequnece of states through that HMM, Viterbi returns the most likely sequence of states *)

(* 
(* *)



    
val _ = export_theory();
